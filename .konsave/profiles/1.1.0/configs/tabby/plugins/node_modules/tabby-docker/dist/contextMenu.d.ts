import { ToastrService } from 'ngx-toastr';
import { MenuItemOptions } from 'tabby-core';
import { ElectronService, ElectronHostWindow } from 'tabby-electron';
import { BaseTerminalTabComponent, TerminalContextMenuItemProvider } from 'tabby-terminal';
import './styles.scss';
export declare class SaveOutputContextMenu extends TerminalContextMenuItemProvider {
    private toastr;
    private electron;
    private hostWindow;
    weight: number;
    constructor(toastr: ToastrService, electron: ElectronService, hostWindow: ElectronHostWindow);
    getItems(tab: BaseTerminalTabComponent): Promise<MenuItemOptions[]>;
    start(tab: BaseTerminalTabComponent): void;
}
