/// <reference types="node" />
import { Injector } from '@angular/core';
import { BaseSession } from 'tabby-terminal';
import { DockerProfile } from './profiles';
export declare class DockerSession extends BaseSession {
    private profile;
    private docker;
    private process;
    private container;
    constructor(injector: Injector, profile: DockerProfile);
    start(): Promise<void>;
    resize(columns: number, rows: number): void;
    write(data: Buffer): void;
    kill(): void;
    gracefullyKillProcess(): Promise<void>;
    supportsWorkingDirectory(): boolean;
    getWorkingDirectory(): Promise<null>;
}
