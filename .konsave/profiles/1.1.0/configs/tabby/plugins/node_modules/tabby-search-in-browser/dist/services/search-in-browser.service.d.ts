import { AppService, BaseTabComponent, ConfigService, HotkeysService, PlatformService } from 'tabby-core';
export declare class SearchInBrowserService {
    private readonly appSerivce;
    private readonly hotkeyService;
    private readonly platformService;
    private readonly configService;
    constructor(appSerivce: AppService, hotkeyService: HotkeysService, platformService: PlatformService, configService: ConfigService);
    /**
     * Description. Subscribes for search in browser hotkey
     */
    init(): void;
    /**
     * Opens selected text in system default browser using default search engine
     */
    openSelectedTextInBrowser(selectedText: string): void;
    /**
     * Returns text selected by user
     */
    getSelectedText(tab: BaseTabComponent): string;
    private searchForSelections;
}
