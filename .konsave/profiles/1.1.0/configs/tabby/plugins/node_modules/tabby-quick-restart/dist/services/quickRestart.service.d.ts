import { LogService, HotkeysService, HostAppService } from 'tabby-core';
export declare class QuickRestartService {
    private app;
    private hotkeys;
    private logger;
    constructor(app: HostAppService, hotkeys: HotkeysService, log: LogService);
    init(): void;
}
