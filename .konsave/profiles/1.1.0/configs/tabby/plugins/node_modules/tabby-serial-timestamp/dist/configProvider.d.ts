import { ConfigProvider } from 'tabby-core';
/** @hidden */
export declare class SerialTimestampConfigProvider extends ConfigProvider {
    defaults: {
        serialTimestampPlugin: {
            timestamp: string;
        };
    };
}
