import { ConfigProvider } from 'tabby-core';
/** @hidden */
export declare class QuickRestartConfigProvider extends ConfigProvider {
    defaults: {
        quickRestart: {};
        hotkeys: {
            'quick-restart': string[];
        };
    };
    platformDefaults: {};
}
