/// <reference types="node" />
import { SessionMiddleware } from 'tabby-terminal';
import { Logger, ConfigService } from 'tabby-core';
export declare class SerialTimestampMiddleware extends SessionMiddleware {
    private config;
    private logger;
    private startOfLine;
    private formatChanged;
    private currentTimestampFormat;
    constructor(config: ConfigService, parentLogger: Logger);
    feedFromSession(data: Buffer): void;
    close(): void;
    private updateTimestampFormat;
    private getDate;
}
