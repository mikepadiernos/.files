import { TerminalDecorator, BaseTerminalTabComponent } from 'tabby-terminal';
import { Logger, LogService, ConfigService } from 'tabby-core';
export declare class SerialTimestampDecorator extends TerminalDecorator {
    logger: Logger;
    config: ConfigService;
    constructor(configService: ConfigService, log: LogService);
    attach(terminal: BaseTerminalTabComponent<any>): void;
    private attachToSession;
}
