import { SEARCH_ENGINES } from './interfaces/interfaces';
import { ConfigProvider } from 'tabby-core';
export declare class SearchInBrowerProvider extends ConfigProvider {
    defaults: {
        searchInBrowserPlugin: {
            defaultEngine: SEARCH_ENGINES;
        };
        hotkeys: {
            "search-in-browser": any[];
        };
    };
}
