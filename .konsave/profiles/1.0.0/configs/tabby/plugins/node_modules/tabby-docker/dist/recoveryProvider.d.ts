import { TabRecoveryProvider, NewTabParameters, RecoveryToken } from 'tabby-core';
import { DockerTabComponent } from './components/dockerTab.component';
/** @hidden */
export declare class RecoveryProvider extends TabRecoveryProvider<DockerTabComponent> {
    applicableTo(recoveryToken: RecoveryToken): Promise<boolean>;
    recover(recoveryToken: RecoveryToken): Promise<NewTabParameters<DockerTabComponent>>;
}
