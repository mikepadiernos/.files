import { DockerService } from 'services/docker.service';
import { BaseComponent, ConfigService } from 'tabby-core';
/** @hidden */
export declare class DockerSettingsTabComponent extends BaseComponent {
    config: ConfigService;
    private docker;
    true: any;
    connected: boolean;
    constructor(config: ConfigService, docker: DockerService);
}
