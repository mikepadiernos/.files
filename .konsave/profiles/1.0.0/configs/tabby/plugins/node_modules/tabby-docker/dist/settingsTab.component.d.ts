import { ConfigService } from 'tabby-core';
import { ElectronHostWindow, ElectronService } from 'tabby-electron';
/** @hidden */
export declare class SaveOutputSettingsTabComponent {
    config: ConfigService;
    private electron;
    private hostWindow;
    constructor(config: ConfigService, electron: ElectronService, hostWindow: ElectronHostWindow);
    pickDirectory(): Promise<void>;
}
