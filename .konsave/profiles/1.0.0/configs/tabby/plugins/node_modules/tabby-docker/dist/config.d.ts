import { ConfigProvider } from 'tabby-core';
/** @hidden */
export declare class DockerConfigProvider extends ConfigProvider {
    defaults: {
        docker: {
            host: any;
            port: any;
            socket: any;
        };
    };
    platformDefaults: {};
}
