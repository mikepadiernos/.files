import { ProfileSettingsComponent, SelectorService } from 'tabby-core';
import { DockerProfile } from '../profiles';
import { Container, DockerService, Image } from '../services/docker.service';
/** @hidden */
export declare class DockerProfileSettingsComponent implements ProfileSettingsComponent<DockerProfile> {
    private selector;
    profile: DockerProfile;
    images: Image[];
    containers: Container[];
    startMode: string;
    constructor(docker: DockerService, selector: SelectorService);
    ngOnInit(): void;
    updateStartMode(): void;
    getImageDescription(): string;
    pickImage(): Promise<void>;
    clearContainerID(): void;
}
