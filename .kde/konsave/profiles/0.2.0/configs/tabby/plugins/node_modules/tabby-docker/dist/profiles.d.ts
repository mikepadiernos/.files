import { BaseTabComponent, NewTabParameters, PartialProfile, Profile, ProfileProvider } from 'tabby-core';
import { DockerProfileSettingsComponent } from './components/dockerProfileSettings.component';
import { DockerService } from './services/docker.service';
export interface DockerProfileOptions {
    containerID?: string;
    containerName?: string;
    imageID?: string;
    command: string;
}
export interface DockerProfile extends Profile {
    type: 'docker';
    options: DockerProfileOptions;
}
export declare class DockerProfileProvider extends ProfileProvider<DockerProfile> {
    private docker;
    id: string;
    name: string;
    weight: number;
    settingsComponent: typeof DockerProfileSettingsComponent;
    configDefaults: {
        options: {
            containerID: any;
            containerName: any;
            imageID: any;
            command: any;
        };
    };
    constructor(docker: DockerService);
    getBuiltinProfiles(): Promise<PartialProfile<DockerProfile>[]>;
    getNewTabParameters(profile: DockerProfile): Promise<NewTabParameters<BaseTabComponent>>;
    getDescription(_profile: PartialProfile<DockerProfile>): string;
}
