import { BaseTabComponent, MenuItemOptions, TabContextMenuItemProvider } from 'tabby-core';
import { SearchInBrowserService } from './services/search-in-browser.service';
export declare class SearchInBrowserContextMenuProvider extends TabContextMenuItemProvider {
    private readonly searchInBrowserSerivce;
    constructor(searchInBrowserSerivce: SearchInBrowserService);
    getItems(tab: BaseTabComponent): Promise<MenuItemOptions[]>;
}
