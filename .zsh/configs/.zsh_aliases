# drush
alias drush='~/public_html/vendor/bin/drush'

# zsh-eza
alias li='la --icons=always'

# lazydocker
alias lzd='lazydocker'

# lazygit
alias lzg='lazygit'

# neomutt
alias nmt='neomutt'

# neovim
# alias nvim='$NEOVIM/nvim'
alias vim='nvim'
alias vm='nvim'

# yazi — use Chafa (half-block) renderer inside VS Code since the IIP adapter
# reports zero pixel dimensions; unset the two env vars that trigger VS Code detection
function yazi() {
    if [[ "$TERM_PROGRAM" == "vscode" ]]; then
        env -u VSCODE_INJECTION -u TERM_PROGRAM command yazi "$@"
    else
        command yazi "$@"
    fi
}
alias yz='yazi'
