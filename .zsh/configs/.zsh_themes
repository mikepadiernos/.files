source_if_exists() {
	local file="$1"
	[[ -f "$file" ]] && source "$file"
}

# source_if_exists "$ZSH_THEMES"/minimal.zsh-theme

# shellcheck source=/dev/null
source_if_exists "$ZSH_THEMES"/typewritten.zsh-theme
